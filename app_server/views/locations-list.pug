extends layout
include _includes/sharedHTMLfunctions

block content
  // 🔽🔽🔽 [수정] Express 위치 목록 코드를 삭제하고 Angular 컴포넌트 삽입 🔽🔽🔽
  .row.banner
    .col-12
      h1= pageHeader.title
      small &nbsp;#{pageHeader.strapLine}

  .row
    .col-12.col-md-8
      app-home-list

    .col-12.col-md-4
      p.lead= sidebar
  // 🔼🔼🔼 [수정] Express 위치 목록 코드를 삭제하고 Angular 컴포넌트 삽입 🔼🔼🔼

block scripts
  // 🔽🔽🔽 [추가] layout.pug에 정의된 block scripts를 오버라이드 🔽🔽🔽
  // Angular 빌드 파일들을 참조합니다.
  script(src='/build/main.js')
  script(src='/build/polyfills.js')
  script(src='/build/runtime.js')
  // 🔼🔼🔼 [추가] layout.pug에 정의된 block scripts를 오버라이드 🔼🔼🔼
